# Инструкция по обновлению RasCam на Raspberry Pi

## Шаг 1: Push изменений на GitHub (на вашем компьютере)

```bash
# Убедитесь, что вы находитесь в директории проекта
cd /Users/a1/Documents/VibeCoding/RasCam

# Отправьте изменения на GitHub
git push origin main
```

## Шаг 2: Подключитесь к Raspberry Pi

```bash
# Замените IP на адрес вашей малинки
ssh pi@192.168.X.X
```

## Шаг 3: Остановите RasCam (на Raspberry Pi)

```bash
# Если RasCam запущен как systemd сервис
sudo systemctl stop rascam

# Или если запущен вручную, нажмите Ctrl+C в терминале
```

## Шаг 4: Обновите код (на Raspberry Pi)

```bash
# Перейдите в директорию проекта
cd ~/RasCam
# или
cd /path/to/your/RasCam

# Сохраните текущие настройки (на всякий случай)
cp config.json config.json.backup

# Получите обновления с GitHub
git pull origin main
```

Вы должны увидеть примерно такой вывод:
```
Updating XXXXXXX..7b13986
Fast-forward
 CHANGELOG_SENSITIVITY.md                     | XXX ++++++++++++++++++
 README.md                                    |   X +-
 SENSITIVITY_GUIDE.md                         | XXX ++++++++++++++++++
 SENSITIVITY_QUICKSTART.md                    | XXX ++++++++++++++++++
 examples/sensitivity_examples.py             | XXX ++++++++++++++++++
 motion_detector.py                           |  XX ++++++++++++++++
 web_interface/app.py                         |  XX ++++++++++++++++
 web_interface/templates/index.html           | XXX ++++++++++++++++++
 8 files changed, XXX insertions(+), X deletions(-)
```

## Шаг 5: Проверьте обновление

```bash
# Проверьте, что новые файлы появились
ls -la SENSITIVITY_*.md
ls -la examples/sensitivity_examples.py

# Проверьте, что изменения применились
git log --oneline -1
```

Вы должны увидеть последний коммит:
```
7b13986 Add motion detector sensitivity control feature
```

## Шаг 6: Запустите RasCam

```bash
# Если используете systemd
sudo systemctl start rascam
sudo systemctl status rascam

# Или запустите вручную
python3 surveillance.py
```

## Шаг 7: Проверьте работу новой функции

### Через веб-интерфейс:
1. Откройте в браузере: `http://IP_МАЛИНКИ:5000`
2. Найдите панель **"Чувствительность детектора"**
3. Попробуйте изменить уровень чувствительности

### Через API:
```bash
# Проверьте текущую чувствительность
curl http://localhost:5000/api/motion/sensitivity

# Установите высокую чувствительность
curl -X POST http://localhost:5000/api/motion/sensitivity \
  -H "Content-Type: application/json" \
  -d '{"sensitivity": "high"}'
```

### Проверьте логи:
```bash
tail -f logs/surveillance.log
```

Вы должны увидеть строки типа:
```
2026-02-16 XX:XX:XX - motion_detector - INFO - Чувствительность установлена 'high' (порог=4.0)
```

## Шаг 8: Проверьте сохранение настроек

```bash
# Проверьте, что threshold сохранился в config.json
cat config.json | grep threshold
```

## Возможные проблемы и решения

### Проблема: "error: Your local changes would be overwritten by merge"

**Решение:**
```bash
# Сохраните ваши изменения
git stash

# Обновите код
git pull origin main

# Восстановите ваши изменения (если нужно)
git stash pop
```

### Проблема: Веб-интерфейс не показывает новую панель

**Решение:**
```bash
# Очистите кеш браузера или откройте в режиме инкогнито
# Или принудительно обновите страницу: Ctrl+Shift+R (или Cmd+Shift+R на Mac)

# Перезапустите RasCam
sudo systemctl restart rascam
```

### Проблема: API возвращает ошибку 500

**Решение:**
```bash
# Проверьте логи
tail -f logs/surveillance.log

# Убедитесь, что все зависимости на месте
pip3 list | grep -i flask

# Перезапустите систему
sudo systemctl restart rascam
```

### Проблема: Настройки не сохраняются

**Решение:**
```bash
# Проверьте права на файл config.json
ls -la config.json

# Если нужно, дайте права на запись
chmod 644 config.json

# Проверьте, что файл не только для чтения
lsattr config.json
```

## Откат к предыдущей версии (если что-то пошло не так)

```bash
# Остановите RasCam
sudo systemctl stop rascam

# Вернитесь к предыдущему коммиту
git log --oneline  # найдите хеш предыдущего коммита
git checkout <previous_commit_hash>

# Восстановите backup конфига
cp config.json.backup config.json

# Запустите систему
sudo systemctl start rascam
```

## После успешного обновления

Теперь вы можете:

1. **Настроить чувствительность** через веб-интерфейс
2. **Протестировать разные уровни** в ваших условиях
3. **Прочитать руководства**:
   - `SENSITIVITY_QUICKSTART.md` - быстрый старт
   - `SENSITIVITY_GUIDE.md` - полное руководство
4. **Запустить примеры**:
   ```bash
   python3 examples/sensitivity_examples.py
   ```

## Дополнительная информация

- Все изменения: `CHANGELOG_SENSITIVITY.md`
- Быстрый старт: `SENSITIVITY_QUICKSTART.md`
- Полное руководство: `SENSITIVITY_GUIDE.md`
- Примеры кода: `examples/sensitivity_examples.py`

## Контрольный список обновления

- [ ] Остановил RasCam
- [ ] Сделал backup config.json
- [ ] Выполнил git pull
- [ ] Проверил, что файлы обновились
- [ ] Запустил RasCam
- [ ] Открыл веб-интерфейс
- [ ] Увидел панель "Чувствительность детектора"
- [ ] Протестировал изменение чувствительности
- [ ] Проверил, что настройки сохраняются
- [ ] Прочитал SENSITIVITY_QUICKSTART.md

Готово! 🎉
