# Решение проблем RasCam

## ❌ Ошибка: "externally-managed-environment" при установке

### Проблема
При запуске `sudo ./install.sh` на шаге [5/8] появляется ошибка:
```
error: externally-managed-environment
```

Это происходит в Raspberry Pi OS Bookworm, который защищает системный Python.

### ✅ Решение 1: Обновить скрипт установки (Рекомендуется)

Я уже обновил `install.sh`, но если вы клонировали старую версию:

```bash
cd ~/RasCam
git pull origin main
sudo ./install.sh
```

Обновлённый скрипт использует системные пакеты (`apt`) вместо `pip`.

---

### ✅ Решение 2: Использовать виртуальное окружение

Запустите альтернативный скрипт установки:

```bash
cd ~/RasCam
sudo ./install_venv.sh
```

Этот вариант создаёт изолированное виртуальное окружение для Python пакетов.

---

### ✅ Решение 3: Ручная установка (если нужен контроль)

```bash
# 1. Установить системные пакеты
sudo apt update
sudo apt install -y \
    python3-picamera2 \
    python3-opencv \
    python3-numpy \
    python3-flask \
    python3-psutil \
    libcamera-apps \
    ffmpeg

# 2. Установить пакет av (нет в apt)
sudo pip3 install --break-system-packages av

# 3. Продолжить с остальными шагами install.sh
```

---

### ✅ Решение 4: Удалить защиту (НЕ рекомендуется)

⚠️ **Только если вы понимаете риски:**

```bash
sudo rm /usr/lib/python3.11/EXTERNALLY-MANAGED
sudo ./install.sh
```

**Риски:** Может сломать системные пакеты при обновлении ОС.

---

## 🔍 Проверка установки

После любого решения проверьте:

```bash
# Проверить импорты
python3 -c "import picamera2; import flask; import av; print('OK')"

# Или запустить тест
cd ~/RasCam
python3 test_components.py
```

---

## 🐛 Другие частые проблемы

### Камера не обнаружена

```bash
# Проверка
libcamera-hello --list-cameras

# Если не видит
sudo raspi-config
# Interface Options → Camera → Enable
sudo reboot
```

### Недостаточно GPU памяти

```bash
# Проверить
vcgencmd get_mem gpu

# Если < 128MB
sudo nano /boot/config.txt
# Добавить: gpu_mem=128
sudo reboot
```

### MediaMTX не запускается

```bash
# Проверить статус
sudo systemctl status mediamtx

# Логи
sudo journalctl -u mediamtx -n 50

# Проверить конфигурацию
sudo nano /etc/mediamtx/mediamtx.yml
```

### RasCam не запускается

```bash
# Логи
sudo journalctl -u rascam -n 100

# Проверить зависимости
cd ~/RasCam
python3 test_components.py

# Проверить конфигурацию
cat config.json
```

### Высокая температура

```bash
# Текущая температура
vcgencmd measure_temp

# Снизить нагрузку в config.json:
{
  "camera": {
    "framerate": 10,  # вместо 15
    "main_resolution": [1280, 720]  # вместо 1080p
  }
}
```

### Нет места на диске

```bash
# Проверка
df -h

# Очистка старых записей
sudo rm /media/usb_ssd/recordings/*.mp4

# Или уменьшить retention_days в config.json
```

---

## 📋 Чек-лист перед запуском

Запустите проверку системы:

```bash
cd ~/RasCam
./check_system.sh
```

Должны быть все галочки ✓. Если есть ошибки ✗ - исправьте перед запуском.

---

## 🆘 Получить помощь

1. Проверьте логи: `sudo journalctl -u rascam -n 100`
2. Запустите тест: `python3 test_components.py`
3. Запустите проверку: `./check_system.sh`
4. Создайте Issue на GitHub с выводом команд выше

**GitHub Issues:** https://github.com/muzapapa-glitch/RasCam/issues
