# Быстрый старт - Настройка чувствительности детектора

## Через веб-интерфейс (рекомендуется)

### Шаг 1: Откройте веб-интерфейс
```
http://IP_ВАШЕГО_PI:5000
```

### Шаг 2: Найдите панель "Чувствительность детектора"
Она находится в правой колонке под статистикой системы.

### Шаг 3: Выберите режим настройки

**Вариант A: Быстрая настройка (рекомендуется для начинающих)**
- Нажмите кнопку **"Низкая"** - для минимума ложных срабатываний
- Нажмите кнопку **"Средняя"** - для баланса (по умолчанию)
- Нажмите кнопку **"Высокая"** - для обнаружения мелких движений
- Нажмите кнопку **"Макс."** - максимальная чувствительность

**Вариант B: Точная настройка**
1. Переместите ползунок или введите число от 0 до 50
2. Нажмите кнопку **"OK"**

**Значения порога:**
- `0-3`: Очень высокая чувствительность (много ложных срабатываний)
- `4-8`: Оптимально для большинства случаев ✓
- `9-15`: Пониженная (для шумных условий)
- `16+`: Очень низкая (только крупные объекты)

## Через API

### Получить текущее значение
```bash
curl http://IP:5000/api/motion/sensitivity
```

### Установить предустановленный уровень
```bash
# Низкая чувствительность
curl -X POST http://IP:5000/api/motion/sensitivity \
  -H "Content-Type: application/json" \
  -d '{"sensitivity": "low"}'

# Средняя (по умолчанию)
curl -X POST http://IP:5000/api/motion/sensitivity \
  -H "Content-Type: application/json" \
  -d '{"sensitivity": "medium"}'

# Высокая
curl -X POST http://IP:5000/api/motion/sensitivity \
  -H "Content-Type: application/json" \
  -d '{"sensitivity": "high"}'
```

### Установить точное значение
```bash
curl -X POST http://IP:5000/api/motion/threshold \
  -H "Content-Type: application/json" \
  -d '{"threshold": 5.5}'
```

## В Python коде

```python
from surveillance import SurveillanceSystem

# Создать систему
system = SurveillanceSystem("config.json")
system.initialize()

# Получить детектор
detector = system.motion_detector

# Установить предустановленный уровень
detector.set_sensitivity('high')

# Или установить точное значение
detector.update_threshold(5.5)

# Проверить текущий уровень
print(f"Чувствительность: {detector.get_sensitivity_level()}")
print(f"Порог: {detector.threshold}")
```

## Быстрые рецепты

### 🏠 Для помещения
```bash
curl -X POST http://IP:5000/api/motion/sensitivity \
  -H "Content-Type: application/json" \
  -d '{"sensitivity": "medium"}'
```

### 🌳 Для улицы (деревья, ветер)
```bash
curl -X POST http://IP:5000/api/motion/threshold \
  -H "Content-Type: application/json" \
  -d '{"threshold": 12.0}'
```

### 🌙 Для ночной съемки
```bash
curl -X POST http://IP:5000/api/motion/threshold \
  -H "Content-Type: application/json" \
  -d '{"threshold": 12.0}'
```

### 🔍 Максимальная чувствительность (для тестов)
```bash
curl -X POST http://IP:5000/api/motion/sensitivity \
  -H "Content-Type: application/json" \
  -d '{"sensitivity": "very_high"}'
```

## Проверка результата

### Смотрим в веб-интерфейсе
Текущий порог отображается в панели "Чувствительность детектора"

### Проверяем через API
```bash
curl http://IP:5000/api/status | grep -A 5 motion
```

### Смотрим в конфиге
```bash
cat config.json | grep threshold
```

## Советы

✓ **Начинайте со средней чувствительности** и корректируйте по результатам

✓ **Слишком много записей?** → Уменьшите чувствительность (увеличьте порог)

✓ **Пропускает события?** → Увеличьте чувствительность (уменьшите порог)

✓ **Тестируйте в реальных условиях** - днём и ночью, при разном освещении

✓ **Используйте зоны детекции** для исключения проблемных областей (деревья, дороги)

## Автоматическое сохранение

Все изменения автоматически сохраняются в `config.json` и применяются при перезапуске системы.

## Дополнительная информация

Полное руководство: [SENSITIVITY_GUIDE.md](SENSITIVITY_GUIDE.md)
Примеры кода: [examples/sensitivity_examples.py](examples/sensitivity_examples.py)
