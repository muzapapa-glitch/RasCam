# Следующие шаги - Обновление RasCam на Raspberry Pi

## ✅ Готово на вашем компьютере

- Функция чувствительности детектора движения реализована
- Все файлы закоммичены в Git
- Изменения отправлены на GitHub (2 коммита)

## 📋 Что нужно сделать на Raspberry Pi

### Шаг 1: Подключитесь к малинке

```bash
ssh pi@192.168.X.X
```
*Замените X.X на IP адрес вашей малинки*

### Шаг 2: Остановите RasCam

```bash
# Если запущен как сервис:
sudo systemctl stop rascam

# Если запущен вручную:
# Нажмите Ctrl+C в окне с запущенным процессом
```

### Шаг 3: Обновите код

```bash
cd ~/RasCam  # или путь к вашей установке

# Сохраните конфиг (на всякий случай)
cp config.json config.json.backup

# Получите обновления
git pull origin main
```

### Шаг 4: Запустите RasCam

```bash
# Если используете systemd:
sudo systemctl start rascam

# Или вручную:
python3 surveillance.py
```

### Шаг 5: Проверьте работу

Откройте веб-интерфейс: `http://IP_МАЛИНКИ:5000`

Вы должны увидеть новую панель **"Чувствительность детектора"** в правой колонке.

## 🎯 Быстрый тест

```bash
# На малинке или с вашего компьютера:
curl http://IP_МАЛИНКИ:5000/api/motion/sensitivity
```

Должен вернуть:
```json
{
  "sensitivity": "medium",
  "threshold": 7.0
}
```

Попробуйте изменить:
```bash
curl -X POST http://IP_МАЛИНКИ:5000/api/motion/sensitivity \
  -H "Content-Type: application/json" \
  -d '{"sensitivity": "high"}'
```

## 📖 Документация

После обновления на малинке будут доступны:

- **SENSITIVITY_QUICKSTART.md** - быстрый старт (начните с этого!)
- **SENSITIVITY_GUIDE.md** - полное руководство
- **UPDATE_INSTRUCTIONS.md** - детальная инструкция по обновлению
- **examples/sensitivity_examples.py** - примеры использования

## 🔍 Что изменилось

### Новые файлы:
```
RasCam/
├── CHANGELOG_SENSITIVITY.md          # Список изменений
├── SENSITIVITY_GUIDE.md              # Полное руководство
├── SENSITIVITY_QUICKSTART.md         # Быстрый старт
├── UPDATE_INSTRUCTIONS.md            # Инструкция по обновлению
└── examples/
    └── sensitivity_examples.py       # Примеры кода
```

### Изменённые файлы:
```
├── motion_detector.py                # +42 строки (новые методы)
├── web_interface/app.py              # +95 строк (API endpoints)
├── web_interface/templates/index.html # +144 строки (UI + JS)
└── README.md                         # Обновлено описание
```

## 🚀 Как использовать

### Через веб-интерфейс (рекомендуется):
1. Откройте `http://IP:5000`
2. Найдите панель "Чувствительность детектора"
3. Нажмите одну из кнопок: **Низкая**, **Средняя**, **Высокая**, **Макс.**

### Через API:
```bash
# Получить текущее значение
curl http://IP:5000/api/motion/sensitivity

# Установить высокую чувствительность
curl -X POST http://IP:5000/api/motion/sensitivity \
  -H "Content-Type: application/json" \
  -d '{"sensitivity": "high"}'
```

## 💡 Рекомендации

**Для помещения:** Начните с уровня **Средняя**

**Для улицы:** Используйте уровень **Низкая** или threshold 10-15

**Для ночи:** Threshold 10-15 (компенсация шума матрицы)

## ❓ Если что-то не работает

1. **Проверьте логи:**
   ```bash
   tail -f logs/surveillance.log
   ```

2. **Перезапустите систему:**
   ```bash
   sudo systemctl restart rascam
   ```

3. **Очистите кеш браузера** и обновите страницу: `Ctrl+Shift+R`

4. **Проверьте версию кода:**
   ```bash
   cd ~/RasCam
   git log --oneline -2
   ```

   Должны быть коммиты:
   ```
   f54c00a Add update instructions for Raspberry Pi
   7b13986 Add motion detector sensitivity control feature
   ```

## 📞 Детальная помощь

Полная инструкция по обновлению: **UPDATE_INSTRUCTIONS.md**

Она содержит:
- Пошаговое руководство
- Решение возможных проблем
- Инструкцию по откату
- Контрольный список

## ✨ После обновления

Попробуйте разные уровни чувствительности и найдите оптимальный для ваших условий:

1. Начните с **Средняя**
2. Понаблюдайте за работой детектора
3. Если много ложных срабатываний → **Низкая**
4. Если пропускает события → **Высокая**
5. Сохраните оптимальные настройки (они автоматически сохранятся в config.json)

Удачи! 🎉
